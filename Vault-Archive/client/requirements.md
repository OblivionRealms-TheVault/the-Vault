## Packages
framer-motion | Essential for the glitch effects, hover animations, and page transitions
clsx | Utility for conditional class names
tailwind-merge | Utility for merging tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  mono: ["'Fira Code'", "monospace"],
  display: ["'Oswald'", "sans-serif"], // Strong, condensed font for headers
  body: ["'Fira Code'", "monospace"], // Terminal look for body
}
